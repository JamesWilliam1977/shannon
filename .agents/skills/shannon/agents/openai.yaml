interface:
  display_name: "Shannon"
  short_description: "Repo-specific patterns and workflows for shannon"
  default_prompt: "Use the shannon repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true